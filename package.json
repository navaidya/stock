{
  "name": "stock-analysis-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "test": "npm run spec:check && vitest run",
    "test:watch": "vitest",
    "spec:check": "node scripts/check-specs.mjs",
    "collect": "node --experimental-strip-types scripts/collect.mjs",
    "brief": "node --experimental-strip-types scripts/brief.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "latest",
    "astro": "latest",
    "yaml": "^2.9.0",
    "zod": "latest"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "latest",
    "typescript": "^6.0.3",
    "vitest": "latest"
  }
}
